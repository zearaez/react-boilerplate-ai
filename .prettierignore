node_modules/
dist/
build/
coverage/
.turbo/
.expo/
pnpm-lock.yaml
CHANGELOG.md

# Generated by `pnpm tokens:sync` — the generator owns the formatting.
apps/web/src/global.css
apps/mobile/global.css

# Generated by `msw init`
apps/web/public/mockServiceWorker.js

# Generated by the Expo CLI, which rewrites it with a trailing space and no final
# newline — so `expo install` / `expo start` silently un-formats it and the next
# `format:check` fails on a file whose own first line says not to edit it.
apps/mobile/expo-env.d.ts

# Generator templates: TypeScript with Handlebars mustaches. Prettier picks them up
# by the .hbs extension and tries to parse them as Handlebars-flavoured HTML, which
# they are not — `Promise<{{pascalCase name}}Page>` is not an element tagname.
# Their formatting is validated the only way that means anything: by generating a
# feature and linting the output.
turbo/generators/templates/
