{
  "name": "@repo/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Design tokens: one source of truth for colour, radius and the Tailwind presets",
  "//": "Consumed as raw TypeScript source. No build step — Metro and Vite both transpile TS, and a dist/ would need building before every lint and typecheck.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./preset": "./src/preset.ts",
    "./contrast": "./src/contrast.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "generate": "tsx scripts/generate.ts"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "tailwindcss": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
