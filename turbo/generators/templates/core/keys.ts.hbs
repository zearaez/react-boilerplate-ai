import type { {{pascalCase plural}}ListParams } from './schemas';

/**
 * GENERATED by `pnpm gen feature`.
 *
 * The nesting is what gives precise invalidation: invalidating `lists()` leaves
 * individual details cached. Never inline a key array at a call site.
 */
export const {{camelCase name}}Keys = {
  all: ['{{plural}}'] as const,
  lists: () => [...{{camelCase name}}Keys.all, 'list'] as const,
  list: (params: {{pascalCase plural}}ListParams) => [...{{camelCase name}}Keys.lists(), params] as const,
  details: () => [...{{camelCase name}}Keys.all, 'detail'] as const,
  detail: (id: string) => [...{{camelCase name}}Keys.details(), id] as const,
};
