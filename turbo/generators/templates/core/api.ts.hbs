import { getApiClient } from '../../api/client';
import { parseRequestBody, toApiError } from '../../api/errors';

import {
  type Create{{pascalCase name}}Input,
  type {{pascalCase name}},
  type {{pascalCase name}}Page,
  type {{pascalCase plural}}ListParams,
  type Update{{pascalCase name}}Input,
  create{{pascalCase name}}InputSchema,
  {{camelCase name}}PageSchema,
  {{camelCase name}}Schema,
  update{{pascalCase name}}InputSchema,
} from './schemas';

/** GENERATED by `pnpm gen feature`. Every function: call, parse, toApiError. */

export async function list{{pascalCase plural}}(params: {{pascalCase plural}}ListParams): Promise<{{pascalCase name}}Page> {
  try {
    const response = await getApiClient().get('{{apiPath}}', {
      params: {
        page: params.page,
        pageSize: params.pageSize,
        ...(params.search ? { search: params.search } : {}),
      },
    });
    return {{camelCase name}}PageSchema.parse(response.data);
  } catch (error) {
    throw toApiError(error);
  }
}

export async function get{{pascalCase name}}(id: string): Promise<{{pascalCase name}}> {
  try {
    const response = await getApiClient().get(`{{apiPath}}/${id}`);
    return {{camelCase name}}Schema.parse(response.data);
  } catch (error) {
    throw toApiError(error);
  }
}

export async function create{{pascalCase name}}(input: Create{{pascalCase name}}Input): Promise<{{pascalCase name}}> {
  try {
    const body = parseRequestBody(create{{pascalCase name}}InputSchema, input);
    const response = await getApiClient().post('{{apiPath}}', body);
    return {{camelCase name}}Schema.parse(response.data);
  } catch (error) {
    throw toApiError(error);
  }
}

export async function update{{pascalCase name}}(
  id: string,
  input: Update{{pascalCase name}}Input,
): Promise<{{pascalCase name}}> {
  try {
    const body = parseRequestBody(update{{pascalCase name}}InputSchema, input);
    const response = await getApiClient().patch(`{{apiPath}}/${id}`, body);
    return {{camelCase name}}Schema.parse(response.data);
  } catch (error) {
    throw toApiError(error);
  }
}

export async function delete{{pascalCase name}}(id: string): Promise<void> {
  try {
    await getApiClient().delete(`{{apiPath}}/${id}`);
  } catch (error) {
    throw toApiError(error);
  }
}
