import { type {{pascalCase name}}, useTranslation } from '@repo/core';

import { Card, CardContent, CardHeader } from '~/components/ui/card';
import { Text } from '~/components/ui/text';

/**
 * GENERATED by `pnpm gen feature`.
 * Counterpart: apps/web/src/features/{{plural}}/{{name}}-card.tsx — keep props in sync.
 */
export interface {{pascalCase name}}CardProps {
  {{camelCase name}}: {{pascalCase name}};
}

export function {{pascalCase name}}Card({ {{camelCase name}} }: {{pascalCase name}}CardProps) {
  const { t } = useTranslation();

  return (
    <Card>
      <CardHeader>
        <Text className="font-semibold leading-snug">{ {{camelCase name}}.title }</Text>
      </CardHeader>
      <CardContent>
        {/* TODO: render the real fields. Strings must go through t(). */}
        <Text className="text-sm text-muted-foreground">{t('common.loading')}</Text>
      </CardContent>
    </Card>
  );
}
