import { useNavigate } from 'react-router';

import { useCreate{{pascalCase name}}, useTranslation } from '@repo/core';

import { {{pascalCase name}}Form } from './{{name}}-form';

/** GENERATED by `pnpm gen feature`. */
export function {{pascalCase name}}CreatePage() {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const create = useCreate{{pascalCase name}}();

  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-semibold tracking-tight">{t('posts.new')}</h1>

      <{{pascalCase name}}Form
        submitLabel={t('posts.create')}
        isSubmitting={create.isPending}
        error={create.error}
        onCancel={() => {
          void navigate(-1);
        }}
        onSubmit={(values) => {
          create.mutate(values, {
            onSuccess: (created) => {
              void navigate(`/{{plural}}/${created.id}`, { replace: true });
            },
          });
        }}
      />
    </div>
  );
}
