export interface Mock{{pascalCase name}} {
  id: string;
  title: string;
  createdAt: string;
}

/**
 * GENERATED by `pnpm gen feature`.
 *
 * Deterministic, not random — a flaky fixture is a flaky test suite. 23 records
 * so a pageSize of 10 gives a short final page, which is where off-by-one bugs
 * in `hasMore` show up.
 */
export const TOTAL_SEEDED_{{constantCase plural}} = 23;

export const initial{{pascalCase plural}}: Mock{{pascalCase name}}[] = Array.from(
  { length: TOTAL_SEEDED_{{constantCase plural}} },
  (_unused, index) => ({
    id: `{{name}}-${String(index + 1).padStart(3, '0')}`,
    title: `Seeded {{name}} #${String(index + 1)}`,
    createdAt: new Date(Date.UTC(2026, 0, 1, 9) - index * 3_600_000).toISOString(),
  }),
);
