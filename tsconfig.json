{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@repo/config/tsconfig/node.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["scripts/**/*.ts", "scripts/**/*.mjs", "turbo/**/*.ts", "vitest.config.ts"],
  "exclude": ["node_modules", "apps", "packages", "dist", "coverage", ".turbo"]
}
